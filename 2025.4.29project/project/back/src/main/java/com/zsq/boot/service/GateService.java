package com.zsq.boot.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.zsq.boot.entity.Gate;

public interface GateService extends IService<Gate> {
    // 继承自 IService，已经有 list() 方法
} 